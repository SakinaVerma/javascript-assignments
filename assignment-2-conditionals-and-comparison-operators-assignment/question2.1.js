if (true) {
    console.log('always executed');
    else {
        console.log('never executed');
    }
}

//Output : Unexpected token 'else'(syntax error because the if block is not closed before else.)